services:
  web:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
      target: runner
    command: ["node", "apps/web/server.js"]
    ports:
      - "3000:3000"
    volumes: []
    environment:
      - NODE_ENV=production
